# LedgerLoop v2.1 — Python dependencies
# The reconciliation engine itself uses only the Python standard library.
# These are the external packages needed to run the Controller UI and test suite.

flask>=3.0
pytest>=8.0
